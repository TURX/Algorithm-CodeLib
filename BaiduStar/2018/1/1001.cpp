#include <iostream>
#include <string>
using namespace std;
int T, n, m, k, str;
unsigned short int data[1003];

int main() {
    ios::sync_with_stdio(false);
    cin >> T;
    for(int casNum = 0; casNum < T; casNum++) {
        cout << "Case #" << casNum + 1 << ": ";
        cin >> n >> m >> k;
        for (int t = 0; t < n; t++)
            {
                cin >> str;
                for (int i = l; i < r; i++)
                {
                    for (int j = i; j < r; j++)
                }
            }
    }
    return 0;
}