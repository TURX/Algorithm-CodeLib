#include <iostream>
using namespace std;

int N;

struct Node {
    int x, y, weight;
} M[10003];

inline double calcE(const double& x, const double& y) {
    
}

int main() {
    ios::sync_with_stdio(false);
    cin >> N;
    return 0;
}