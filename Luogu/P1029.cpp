// !!
/*
#include <iostream>
#include <algorithm>
using namespace std;
int temparr[2],numarr[2],looparr[2];
void division(int &a, int &b)
{
	int temp;
	while (b != 0) {
		temp = a % b;
		a = b;
		b = temp;
	}
}
int main() {
	ios::sync_with_stdio(false);
	cin>>temparr[0]>>temparr[1];
	numarr=temparr;
	sort(temparr,temparr+2);
	looparr=temparr;
	division(looparr[0],looparr[1]);
	cout<<
	output("The GCD of ", false); for (int t = 0; t < numarr.Length - 1; t++) output(numarr[t] + ",", false); output(numarr[numarr.Length - 1].ToString(), false); output(" is: " + looparr[0], true);
	int lcm = 0;
	for (int t = 0; t < numarr.Length; t++) lcm *= numarr[t];
	for (int t = 0; t < looparr.Length - 1; t++) lcm /= looparr[0];
	output("The LCM of them is: " + lcm, true);
}
*/
