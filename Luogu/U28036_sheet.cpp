#include <iostream>
using namespace std;

unsigned n, Q, x, y;
unsigned long long sequence[100003];

int main() {
    ios::sync_with_stdio(false);
    cin >> n;
    for(unsigned t = 0; t < n; t++) {
        cin >> sequence[t];
    }
    for(unsigned t = 0; t < 5; t++) {
        
    }
    return 0;
}