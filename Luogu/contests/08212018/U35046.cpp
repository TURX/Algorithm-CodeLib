#include <iostream>
using namespace std;

int a, b, c, d, e, f, g, h, k, x, y;

int main() {
    ios::sync_with_stdio(false);
    cin >> a >> b >> c >> d >> e >> f >> g >> h >> k >> x >> y;
    
    return 0;
}