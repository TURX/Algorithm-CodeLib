#include <iostream>
using namespace std;

long long A, B, ans, speed;

int main() {
    ios::sync_with_stdio(false);
    cin >> A >> B;
    for(long long i = 1; i <= B - A + 1; i++) {
        speed++;
    }
    return 0;
}