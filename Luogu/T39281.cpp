#include <iostream>
#include <string>
using namespace std;

int n, m, k, T;
int x, y, c;
string s;

int main() {
    ios::sync_with_stdio(false);
    cin >> n >> m >> k >> T >> s;
    for(int t = 0; t < m; t++);
}