#include <iostream>
#include <cstdio>
using namespace std;

#define MAXN 10001

int T, N, W, H, X, Y, L;

inline void build(const int& id, const int& l, const int& r) {
    if (l == r) total
}

int main () {
    scanf("%d", &T);
    while (T--) {
        cin >> N >> W >> H;
        for (int t = 0; t < N; t++) {
            cin >> X >> Y >> L;
        }
    }
    return 0;
}