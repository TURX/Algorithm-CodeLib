#include <iostream>
#include <vector>
using namespace std;

int main() {
	#ifdef VSCODE_DBG
		freopen("test.in", "r", stdin);
	#endif
	cin.tie(0)->sync_with_stdio(0);
	int N, L, K;
	cin >> N >> L >> K;
	vector<int> A(N);
	for (int i = 0; i < N; i++)
}
