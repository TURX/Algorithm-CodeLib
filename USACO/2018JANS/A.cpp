#include <cstdio>
#include <iostream>
#include <set>
using namespace std;

int N, a, b, ans;

int main() {
    freopen("planting.in", "r", stdin);
    freopen("planting.out", "w", stdout);
    ios::sync_with_stdio(false);
    cin >> N;
    for(int t = 0; t < N - 1; t++) {
        cin >> a >> b;
    }
    return 0;
}