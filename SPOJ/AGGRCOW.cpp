#include <iostream>
using namespace std;

int main() {
    unsigned T;
    cin >> T;
    while (T--) {
        unsigned N, C;
        cin >> N >> C;
        int X[N];
        for (unsigned i = 0; i < N; i++) {
            cin >> X[i];
        }
        unsigned low = 0, high = 1000000000;
    }
    return 0;
}
