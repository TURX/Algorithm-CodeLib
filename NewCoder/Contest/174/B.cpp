#include <iostream>
using namespace std;

bool flag_minimize;
unsigned int n, a, b; unsigned long long m;

int main() {
    ios::sync_with_stdio(false);
    cin >> flag_minimize >> n >> m;
    while(true) {
        cin >> a;
        if(a == 0) break;
        cin >> b;
    }
    return 0;
}