#include <iostream>
#include <vector>
using namespace std;

struct MS_R {
    
};

int main() {
    int N;
    cin >> N;
    vector<int> A = vector<int>(N);
    for (int i = 0; i < N; i++) cin >> A[i];
    
}
